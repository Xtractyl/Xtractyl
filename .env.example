# ===============================
# 📦 Label Studio and Ollama Setup
# ===============================

LABEL_STUDIO_URL=http://labelstudio:8080  # Should remain unchanged (internal container address)
LABEL_STUDIO_API_TOKEN=                  # Fill in the API token from your Label Studio account
LABEL_STUDIO_PROJECT_ID=                 # Will be automatically set when a project is created

OLLAMA_MODEL=gemma3:12b  # Model must be installed separately; check license terms of the model provider (e.g., Gemma by Google)

LS_USER=changeme@example.com            # Your Label Studio login (email)
LS_PASS=changeme                        # Your Label Studio password

# ===============================
# ⚙️ Model and System Parameters
# ===============================

LLM_TIMEOUT_SECONDS=300                 # Max response time per question (in seconds)
ML_BACKEND_URL=http://ml_backend:6789  # Should remain unchanged (internal container address)

# Prompt for the LLM – make sure it enforces literal extraction
SYSTEM_PROMPT=Du bist ein Extraktionsmodell. Wenn dir ein medizinischer Text und eine konkrete Frage gegeben wird, antwortest du **nur** mit exakt der Textpassage, die im Text steht – ohne Umschreibungen, ohne Interpretation, ohne zusätzliche Worte.\n\nAntwortformat: **Nur der originale Textabschnitt**, ohne Anführungszeichen, ohne JSON, keine Kommentare.\n\nWenn keine passende Passage im Text vorkommt, gib **genau das hier** zurück: <keine Antwort>

# ===============================
# 📊 For Evaluation / Comparison
# ===============================

LABEL_STUDIO_PROJECT_ID_PRED=8          # ID of the project with accepted model predictions
LABEL_STUDIO_PROJECT_ID_GT=17           # ID of the project with ground truth annotations