name: Smoke

on:
  push:
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install smoke test deps (make deps)
        run: make deps
      # make up makes compose up for the orchestrator starting all container except docling

      - name: Start stack (make up)
        run: make up

      # compose up with waiting
      - name: Wait for orchestrator health
        run: |
          for i in {1..60}; do
            if curl -fsS http://localhost:5001/health >/dev/null; then
              echo "Orchestrator is healthy"
              exit 0
            fi
            sleep 2
          done
          echo "Orchestrator did not become healthy in time"
          docker compose ps
          docker compose logs --no-color --tail=200 || true
          exit 1

      - name: Wait for frontend
        run: |
          for i in {1..60}; do
            if curl -fsS http://localhost:5173/ >/dev/null; then
              echo "Frontend is up"
              exit 0
            fi
            sleep 2
          done
          echo "Frontend did not become ready in time"
          docker compose ps
          docker compose logs --no-color --tail=200 frontend || true
          exit 1

      - name: Wait for ML backend health
        run: |
          for i in {1..60}; do
            if curl -fsS http://localhost:6789/health >/dev/null; then
              echo "ML backend is healthy"
              exit 0
            fi
            sleep 2
          done
          echo "ML backend did not become healthy in time"
          docker compose ps
          docker compose logs --no-color --tail=200 || true
          exit 1

      - name: Run smoke tests (make smoke)
        run: make smoke

      - name: Tear down stack (make down)
        if: always()
        run: make down